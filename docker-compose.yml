version: '3.8'

services:
  ideb-app:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ideb-data:/app/data
    environment:
      - DB_PATH=/app/data/ideb.db
      - INPUT_JSON_PATH=/app/memory-bank/input.json
      - SERVER_PORT=8080

volumes:
  ideb-data: